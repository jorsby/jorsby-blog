// No content collections in use — show/episode data lives in src/data/shows.ts
// (types + helpers) + src/data/shows.generated.ts (produced by
// scripts/sync-portfolio.mjs). Kept so astro:content stays satisfied.
export const collections = {};
